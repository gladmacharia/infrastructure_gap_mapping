[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.52.4",
]

[tool.uv.workspace]
members = [
    "etl",
    "backend",
]
